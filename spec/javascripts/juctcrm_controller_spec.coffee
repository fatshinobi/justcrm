describe "Justcrm controller", ->
  #beforeEach ->
    #@controller = new Justcrm.Controllers.JustcrmController()
  #it "defined", ->
  #  expect(@controller).not.toBeNull()
  #it "define people", ->
  #  expect(@controller.people).toBeDefined()
  #it "define person", ->
  #  expect(@controller.person).toBeDefined()    